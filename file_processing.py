import os
import shutil
from audio_processing import check_id3_tags, convert_flac_to_mp3
from config import Config

def process_file(file_path, config):
    # Implementation for processing a file based on its type and metadata

def delete_inactive_folders(folder_activity, config):
    # Implementation for deleting inactive folders